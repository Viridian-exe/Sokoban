#include <stdio.h>
int main() {
    char letter = 'a'; // Exemple de lettre minuscule
    char uppercaseLetter = letter - 32; // Conversion en majuscule

    printf("La majuscule de %c est %c\n", letter, uppercaseLetter);
    return 0;
}
